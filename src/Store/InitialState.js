import listAllAdreses from '../Information/adreses.json' //импорт всего списка адресов офисов


const initinalState = {
    AllAdreses: listAllAdreses,
    NumberActiveCity: 0
}

export default initinalState